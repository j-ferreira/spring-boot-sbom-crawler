spring:
  application:
    name: spring-boot-sbom-crawler
management:
  endpoints:
    web:
      exposure:
        include: health,sbom
  server:
    port: 8091
server:
  port: 8090

feign:
  client:
    config:
      dependency-track:
        url: https://localhost:8081

crawler:
  configurations:
    - id: demo-application
      endpoint: http://localhost:8091/actuator/sbom
      projectId: ffe51202-3285-4f79-8df6-9a7656aa709b
      projectName: spring-boot-sbom-crawler
      projectVersion: PROD


